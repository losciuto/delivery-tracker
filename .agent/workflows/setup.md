---
description: Configura l'ambiente virtuale e installa le dipendenze
---

Per configurare l'ambiente da zero, esegui lo script di setup:

// turbo
1. Esegui lo script di setup:
```bash
./setup.sh
```

2. Verifica le dipendenze installate:
```bash
source .venv/bin/activate && pip list
```
